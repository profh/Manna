<%= form_for(@vote) do |f| %>
  <% if @vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vote.errors.count, "error") %> prohibited this vote from being saved:</h2>

      <ul>
      <% @vote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :deacon_id %><br>
    <%= f.number_field :deacon_id %>
  </div>
  <div class="field">
    <%= f.label :decision %><br>
    <%= f.text_field :decision %>
  </div>
  <div class="field">
    <%= f.label :case_id %><br>
    <%= f.number_field :case_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
